/*
#include <QDebug>

#include "pathfinding.h"
#include "grid.h"

AAPathfinding::AAPathfinding( AAGrid* inAAGrid ) {

    aaGrid = inAAGrid;

    for (auto const& pair: aaGrid->hashesNodeMapping) {
        pair.second->reset();
        qDebug() << "fack" << pair.second->posX;
    }

};
*/
